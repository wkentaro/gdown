Optional cancellation events for `download()` and `cached_download()` interrupt stalled response reads and download waits without shortening network timeouts, raising `DownloadCancelled` while preserving partial-file cleanup.
