Add opt-in `--retries N` and Python `retries=N` for automatic recovery of interrupted file and folder downloads, with bounded backoff and validated byte-range resume; `--continue` still controls reuse of earlier downloads.
